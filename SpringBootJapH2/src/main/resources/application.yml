spring:
  application:
    name: SpringBootJapH2
  jpa:
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  h2:
    console:
      enabled: true
logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace
